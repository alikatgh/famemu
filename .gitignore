build/
build-*/
build_tools/

# clangd / editor index cache (machine-specific)
.cache/
*.cs8
*.ppm
*.bmp
*.f32
.DS_Store
# local-only SNES smoke ROM (game content stays out of the repo)
engines/snes/tests/data/*.sfc
